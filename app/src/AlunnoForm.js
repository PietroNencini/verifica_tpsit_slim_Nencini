import { useState } from 'react';

function AlunnoForm(props) {

    const [nome, setNome] = useState(props.nomeAlunno);
    const [cognome, setCognome] = useState(props.cognomeAlunno);

    
    
}

export default AlunnoForm;